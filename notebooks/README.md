# Notebooks

The production logic lives in `src/` and `sql/`, not notebooks. This directory is intentionally reserved for disposable exploration or interview demonstrations. Any notebook-derived production transformation must be migrated into tested modules before publication.
